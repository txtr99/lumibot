# Phase 01-02: Confirm Market Calendar Bug

## Objective
Verify that Bug #3 (market calendar mismatch) is actually occurring and determine the correct fix.

## Context
@lumibot/brokers/broker.py (lines 115-131 - has auto-detection logic)
@custom_portfolio/strategies/run_portfolio.py (PortfolioStrategy class)

## Tasks

### Task 1: Check Broker Auto-Detection
**Type:** investigate
**Files:** `lumibot/brokers/broker.py`
**Action:**
Review lines 122-126: The broker already has auto-detection for ProjectXData:
```python
if ds_name in ("ProjectXData", "TradovateData"):
    self.market = "us_futures"
```

Determine:
1. What data source is being used in run_portfolio.py?
2. Is the auto-detection being triggered?
3. Why might it be failing?

**Verify:**
- Document the data source class name being used
- Explain if auto-detection should work or why it's failing

### Task 2: Add Debug Logging
**Type:** modify
**Files:** `custom_portfolio/strategies/run_portfolio.py`
**Action:**
Add temporary logging in PortfolioStrategy.initialize() to print:
- `self.broker.market` after initialization
- `self.broker.data_source.__class__.__name__`

**Verify:**
- Logging added
- Can run a short backtest to see the values

### Task 3: Run Short Backtest
**Type:** execute
**Command:** `source venv/bin/activate && BACKTESTING_START=2025-10-01 BACKTESTING_END=2025-10-02 python custom_portfolio/strategies/run_portfolio.py --mode backtest 2>&1 | head -100`
**Action:** Run backtest and capture market value
**Verify:**
- See what market value is actually set
- Confirm if fix is needed

## Verification
- [ ] Know the data source class name
- [ ] Know the current broker.market value
- [ ] Determine if explicit set_market() is needed or if auto-detection should work

## Success Criteria
- Clear understanding of why market calendar is wrong
- Know exactly what fix is needed (if any)

## Output
Create `01-02-SUMMARY.md` with:
- Data source class name
- Current broker.market value
- Required fix (if any)
