# Phase 03-01: Full Backtest Validation

## Objective
Run the same backtest that originally showed the bugs and verify they are fixed.

## Context
All previous phases complete.

## Tasks

### Task 1: Run Original Backtest
**Type:** execute
**Command:** `source venv/bin/activate && BACKTESTING_START=2025-10-01 BACKTESTING_END=2025-10-05 python custom_portfolio/strategies/run_portfolio.py --mode backtest 2>&1`
**Action:** Run the full backtest that originally showed issues
**Verify:**
- Backtest completes without errors
- No lookahead warnings in output

### Task 2: Verify Unrealized P&L Varies
**Type:** verify
**Action:**
Check equity curve or snapshots to confirm:
1. Unrealized P&L is NOT constant at $3,353
2. P&L values change with each iteration
3. No phantom P&L at first iteration (t=0)

**Verify:**
- P&L varies with price changes
- First iteration has $0 unrealized P&L (no positions yet)

### Task 3: Verify Futures Hours
**Type:** verify
**Action:**
Check that backtest covers futures hours:
1. Trades before 09:30 are included in iteration
2. Trades after 16:00 are included in iteration

**Verify:**
- Iterations cover overnight session
- Not limited to NYSE hours

### Task 4: Verify Snapshot Timestamps
**Type:** verify
**Action:**
Check snapshots to confirm:
1. Timestamps match current backtest time
2. No future timestamps in snapshots

**Verify:**
- All timestamps are at or before iteration time

## Verification Checklist
- [ ] Backtest runs without errors
- [ ] No lookahead warnings
- [ ] Unrealized P&L varies (not constant)
- [ ] No phantom P&L at t=0
- [ ] Futures hours covered
- [ ] Snapshot timestamps correct

## Success Criteria
All 6 original bugs are resolved:
- Bug #1: SharedDataManager now filters data via get_data_at_time()
- Bug #2: Equity curve uses filtered data
- Bug #3: Market calendar uses futures hours
- Bug #4: Fallback uses filtered data
- Bug #5: Snapshots use correct timestamps
- Bug #6: No phantom P&L (consequence of #1 + #2 fix)

## Output
Create `03-01-SUMMARY.md` with:
- Before/after comparison
- Confirmation all bugs fixed
- Any remaining issues discovered
- Clean-up: remove temp files (tmp_*.py)
