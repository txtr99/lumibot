# Phase 02-03: Fix Market Calendar (If Needed)

## Objective
Ensure the backtest uses futures market hours instead of NYSE hours.

## Context
@lumibot/brokers/broker.py
@custom_portfolio/strategies/run_portfolio.py

## Pre-Condition
This plan should be executed AFTER Phase 01-02 confirms whether the bug exists.

If Phase 01-02 determines:
- Auto-detection IS working → Skip this phase
- Auto-detection NOT working → Execute this phase

## Tasks

### Task 1: Add Explicit Market Setting (if needed)
**Type:** modify
**Files:** `custom_portfolio/strategies/run_portfolio.py`
**Action:**
Add at the beginning of `PortfolioStrategy.initialize()`:

```python
def initialize(self):
    """Initialize the portfolio manager."""
    # Ensure futures market hours for CME contracts
    self.set_market("us_futures")

    PortfolioStrategy._base_capital = shared_initial_capital
    # ... rest of existing code
```

**Verify:**
- Line added at top of initialize()
- No syntax errors

### Task 2: Verify Market Setting
**Type:** execute
**Command:** `source venv/bin/activate && BACKTESTING_START=2025-10-01 BACKTESTING_END=2025-10-02 python custom_portfolio/strategies/run_portfolio.py --mode backtest 2>&1 | grep -i market`
**Action:** Run short backtest and confirm market is set correctly
**Verify:**
- Output shows "us_futures" or equivalent
- Backtest iterates beyond NYSE hours

## Verification
- [ ] Market explicitly set to futures if needed
- [ ] Backtest covers futures trading hours

## Success Criteria
- Backtest iterates through correct trading hours for futures
- Trades at 04:26, 07:53, etc. are included in iteration

## Output
Create `02-03-SUMMARY.md` with:
- Whether fix was needed
- Confirmation of correct market hours
