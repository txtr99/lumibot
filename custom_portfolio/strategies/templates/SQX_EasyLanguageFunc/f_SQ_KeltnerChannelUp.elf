inputs: Length( numericsimple ), NumATRs( numericsimple ) ;

SQ_KeltnerChannelUp = Average( Close, Length ) + NumATRs * AvgTrueRange( Length );

{ Force series function, since otherwise this function can verify as simple, but needs to be called at every bar to return correct results }
If false then Value10 = SQ_KeltnerChannelUp[1];
