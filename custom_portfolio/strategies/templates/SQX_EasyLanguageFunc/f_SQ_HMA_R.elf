{ Search Tag: WA-HMA }

{ Calculates values of the Hull moving average. }

inputs: 
	double Price( numericseries ), 
	double Length( numericsimple ), 
	ApplyRound(NumericSimple) ; 

Value1 = SQ_HMA(Price, Length);
If ApplyRound = 1 then Value1 = Round(Value1 , 6);
SQ_HMA_R = Value1;
