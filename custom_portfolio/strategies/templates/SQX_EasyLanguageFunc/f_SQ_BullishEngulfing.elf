
SQ_BullishEngulfing = 0;

if ((Open[1] > Close[1]) and (Close > Open) and (Close >= Open[1]) and (Close[1] >= Open) and ((Close - Open) > (Open[1] - Close[1]))) then
	SQ_BullishEngulfing = 1;
	
{ Force series function, since otherwise this function can verify as simple, but needs to be called at every bar to return correct results }
If false then Value10 = SQ_BullishEngulfing[1]
