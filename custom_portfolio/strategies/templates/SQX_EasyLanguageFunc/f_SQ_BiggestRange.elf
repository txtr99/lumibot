inputs: 
	Period( numericsimple );

variables:
	highestIndex( 0 ),
	highestValue( 0 ),
	retVal( 0 );
	
retVal = SQ_Extremes(Range, Period, 1, highestValue, highestIndex);
SQ_BiggestRange = highestValue;

{ Force series function, since otherwise this function can verify as simple, but needs to be called at every bar to return correct results }
If false then Value3 = SQ_BiggestRange[1];
