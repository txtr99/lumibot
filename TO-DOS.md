# Outstanding Todos

## Integrate Order/Position Management into Main Bot Loop - 2025-11-24 22:05

- **Integrate BracketOrderManager and position sync into run_portfolio.py** - Add proper portfolio-level position management using virtual positions with REST API lookups (no SignalR websocket by design) and exchange synchronization across multiple futures markets. **Problem:** Current implementation almost works but lacks robust position/order/signal processing loops. Race conditions can occur when closing positions with active brackets. Virtual positions need reconciliation with exchange. BracketOrderManager exists but isn't integrated into main loop. **Files:** `custom_portfolio/run_portfolio.py` (main entry), `custom_portfolio/multi_strategy_executor_enhanced.py` (trading logic), `lumibot/tools/virtual_position_tracker.py` (virtual positions), `tools/bracket_order_manager.py` (orphan cleanup - ready to integrate), `custom_portfolio/data/futures_metadata.py` (symbol lookups, round_to_tick), `pnl_validation_workflow_notes.md` (validated patterns + integration guide). **Solution:** (1) Integrate BracketOrderManager - call poll_and_cleanup() every 2-5 seconds, register brackets after SL/TP placement. (2) Use race-safe close pattern: cancel brackets FIRST → wait 0.5s → query positions → close → verify flat. (3) Use position type field (type: 1=LONG, 2=SHORT) not size sign for direction. (4) Add price rejection retry for errorCode 2 "outside range". (5) Periodic exchange vs virtual position reconciliation. (6) Build unit tests for all order/position flows. See pnl_validation_workflow_notes.md "Production Integration Checklist" and "Code Patterns for Main Loop Integration" sections for validated patterns.
