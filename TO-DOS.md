# Outstanding Todos

## Remove Bracket Orders to Prevent Duplication - 2025-11-22 00:54

- **Remove bracket order implementation** - Switch to manual-only monitoring for TP/SL exits to eliminate risk of duplicate exit orders. **Problem:** Current system has both ProjectX broker's automatic bracket order handling (spawns child TP/SL orders when parent fills) AND manual monitoring in multi_strategy_executor_enhanced.py that checks bar high/low and creates close orders. This creates race condition where BOTH systems could trigger exit orders for same position, causing duplicate sells that either: (1) remove exposure from another open long, leaving orphaned logic, or (2) push position below 0 unintentionally. Reliability of broker bracket orders is questionable given order submission errors seen. **Files:** `custom_portfolio/multi_strategy_executor_enhanced.py:817-874` (bracket order creation), `custom_portfolio/multi_strategy_executor_enhanced.py:394-427` (manual monitoring), `custom_portfolio/multi_strategy_executor_enhanced.py:631-638` (manual exit trigger), `custom_portfolio/multi_strategy_executor_enhanced.py:846-847,1099-1102` (pending_tp/pending_sl storage). **Solution:** Remove `order_class=Order.OrderClass.BRACKET` and `secondary_limit_price`/`secondary_stop_price` from order creation. Keep only manual monitoring system with `_check_bracket_hit()` checking bar high/low every iteration. Ensures single source of truth for exits with no broker dependency or race conditions.

## Implement Trading Sessions with Calendar - 2025-11-22 00:57

- **Implement session definitions and calendar system** - Add global trading session windows (Australia, Asia, London, New_York, 24/7) with platform rules for Topstep to enforce session boundaries and daily/weekend maintenance windows. **Problem:** Strategies can declare `allowed_sessions` but sessions have no effect because calendar is never instantiated. The `create_calendar()` stub in `run_portfolio.py` returns None, so session enforcement logic in executor is bypassed. Previously disabled Topstep session rules (30-min pre-close order stop, 14:05 MT force-flat, nightly maintenance) need clean reintroduction. **Files:** `custom_portfolio/strategies/portfolio_manager.py` (add TRADING_SESSIONS and PLATFORM_CONFIG definitions), `custom_portfolio/strategies/run_portfolio.py:create_calendar()` (replace stub with actual TradingCalendar construction), `custom_portfolio/multi_strategy_executor_enhanced.py` (confirm existing calendar logic works with cross-midnight sessions), `custom_portfolio/strategies/templates/strategy_template.py` (update docstring to reference TRADING_SESSIONS), `env.example` (add ENFORCE_SESSIONS_IN_BACKTEST flag). **Solution:** Define session windows in Mountain Time (America/Denver) with start/stop_new_orders/force_flat times. Import `lumibot.tools.trading_calendar.TradingCalendar` and `pytz`, build calendar with timezone and platform config, register all sessions. Add env var `ENFORCE_SESSIONS_IN_BACKTEST` (default False) for optional backtest enforcement. Ensure cross-midnight sessions (Australia/Asia 17:00-02:00 MT) handle day boundaries correctly. Platform config captures daily maintenance (14:05-15:00 MT) and weekend windows (close Fri 14:05, open Sun 15:00).
